import { BREAK_LEN, SESSION_LEN, TIME_LEFT } from './default-values'

const initState = {
  breakLength: BREAK_LEN,
  sessionLength: SESSION_LEN,
  timeLeft: TIME_LEFT,
  timerOn: false,
  onSession: true
}

export default initState
